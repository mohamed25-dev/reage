{
  "name": "reage",
  "version": "1.0.0",
  "description": "Image uploading web app",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "cd client && npm run start",
    "server": "node app.js",
    "heroku-postbuild": "cd client && npm install && npm run build",
    "start": "node app.js"
  },
  "author": "mohamed",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "http-errors": "^1.8.0",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.0.9",
    "multer": "^1.4.3"
  },
  "engines": {
    "node": "14.15.4"
  }
}
